#' EMS - REST API
#'
#' This section will provide necessary information about the `CoinAPI EMS REST API` protocol. <br/> This API is also available in the Postman application: <a href=\"https://postman.coinapi.io/\" target=\"_blank\">https://postman.coinapi.io/</a>       <br/><br/> Implemented Standards:    * [HTTP1.0](https://datatracker.ietf.org/doc/html/rfc1945)   * [HTTP1.1](https://datatracker.ietf.org/doc/html/rfc2616)   * [HTTP2.0](https://datatracker.ietf.org/doc/html/rfc7540)     ### Endpoints <table>   <thead>     <tr>       <th>Deployment method</th>       <th>Environment</th>       <th>Url</th>     </tr>   </thead>   <tbody>     <tr>       <td>Managed Cloud</td>       <td>Production</td>       <td>Use <a href=\"#ems-docs-sh\">Managed Cloud REST API /v1/locations</a> to get specific endpoints to each server site where your deployments span</td>     </tr>     <tr>       <td>Managed Cloud</td>       <td>Sandbox</td>       <td><code>https://ems-gateway-aws-eu-central-1-dev.coinapi.io/</code></td>     </tr>     <tr>       <td>Self Hosted</td>       <td>Production</td>       <td>IP Address of the <code>ems-gateway</code> container/excecutable in the closest server site to the caller location</td>     </tr>     <tr>       <td>Self Hosted</td>       <td>Sandbox</td>       <td>IP Address of the <code>ems-gateway</code> container/excecutable in the closest server site to the caller location</td>     </tr>   </tbody> </table>  ### Authentication If the software is deployed as `Self-Hosted` then API do not require authentication as inside your infrastructure, your company is responsible for the security and access controls.  <br/><br/> If the software is deployed in our `Managed Cloud`, there are 2 methods for authenticating with us, you only need to use one:   1. Custom authorization header named `X-CoinAPI-Key` with the API Key  2. Query string parameter named `apikey` with the API Key  3. <a href=\"#certificate\">TLS Client Certificate</a> from the `Managed Cloud REST API` (/v1/certificate/pem endpoint) while establishing a TLS session with us.  #### Custom authorization header You can authorize by providing additional custom header named `X-CoinAPI-Key` and API key as its value. Assuming that your API key is `73034021-THIS-IS-SAMPLE-KEY`, then the authorization header you should send to us will look like: <br/><br/> `X-CoinAPI-Key: 73034021-THIS-IS-SAMPLE-KEY` <aside class=\"success\">This method is recommended by us and you should use it in production environments.</aside> #### Query string authorization parameter You can authorize by providing an additional parameter named `apikey` with a value equal to your API key in the query string of your HTTP request. Assuming that your API key is `73034021-THIS-IS-SAMPLE-KEY` and that you want to request all balances, then your query string should look like this:  <br/><br/> `GET /v1/balances?apikey=73034021-THIS-IS-SAMPLE-KEY` <aside class=\"notice\">Query string method may be more practical for development activities.</aside> 
#'
#' The version of the OpenAPI document: v1
#' Contact: support@coinapi.io
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Orders operations
#' @description OrdersApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ V1OrdersCancelAllPost } \emph{ Cancel all orders request }
#' This request cancels all open orders on single specified exchange.
#'
#' \itemize{
#' \item \emph{ @param } order_cancel_all_request \link{OrderCancelAllRequest}
#' \item \emph{ @returnType } \link{MessageReject} \cr
#'
#'
#' \item status code : 200 | Result
#'
#' \item return type : MessageReject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Input model validation errors.
#'
#' \item return type : ValidationError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 490 | Exchange is unreachable.
#'
#' \item return type : MessageReject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ V1OrdersCancelPost } \emph{ Cancel order request }
#' Request cancel for an existing order. The order can be canceled using the `client_order_id` or `exchange_order_id`.
#'
#' \itemize{
#' \item \emph{ @param } order_cancel_single_request \link{OrderCancelSingleRequest}
#' \item \emph{ @returnType } \link{OrderExecutionReport} \cr
#'
#'
#' \item status code : 200 | The last execution report for the order for which cancelation was requested.
#'
#' \item return type : OrderExecutionReport
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Input model validation errors.
#'
#' \item return type : ValidationError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 490 | Exchange is unreachable.
#'
#' \item return type : MessageReject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ V1OrdersGet } \emph{ Get open orders }
#' Get last execution reports for open orders across all or single exchange.
#'
#' \itemize{
#' \item \emph{ @param } exchange_id character
#' \item \emph{ @returnType } list( \link{OrderExecutionReport} ) \cr
#'
#'
#' \item status code : 200 | Collection of order execution reports.
#'
#' \item return type : array[OrderExecutionReport]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 490 | Filtered exchange is unreachable.
#'
#' \item return type : MessageReject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ V1OrdersHistoryTimeStartTimeEndGet } \emph{ History of order changes }
#' Based on the date range, all changes registered in the orderbook.
#'
#' \itemize{
#' \item \emph{ @param } time_start character
#' \item \emph{ @param } time_end character
#' \item \emph{ @returnType } list( \link{OrderHistory} ) \cr
#'
#'
#' \item status code : 200 | The last execution report of the requested order.
#'
#' \item return type : array[OrderHistory]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Orders log is not configured.
#'
#' \item return type : MessageError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ V1OrdersPost } \emph{ Send new order }
#' This request creating new order for the specific exchange.
#'
#' \itemize{
#' \item \emph{ @param } order_new_single_request \link{OrderNewSingleRequest}
#' \item \emph{ @returnType } \link{OrderExecutionReport} \cr
#'
#'
#' \item status code : 200 | Created
#'
#' \item return type : OrderExecutionReport
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 400 | Input model validation errors.
#'
#' \item return type : ValidationError
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 490 | Exchange is unreachable.
#'
#' \item return type : MessageReject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 504 | Exchange didn't responded in the defined timeout.
#'
#' \item return type : MessageReject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ V1OrdersStatusClientOrderIdGet } \emph{ Get order execution report }
#' Get the last order execution report for the specified order. The requested order does not need to be active or opened.
#'
#' \itemize{
#' \item \emph{ @param } client_order_id character
#' \item \emph{ @returnType } \link{OrderExecutionReport} \cr
#'
#'
#' \item status code : 200 | The last execution report of the requested order.
#'
#' \item return type : OrderExecutionReport
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 404 | The requested order was not found.
#'
#' \item return type : MessageReject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  V1OrdersCancelAllPost  ####################
#'
#' library(openapi)
#' var_order_cancel_all_request <- OrderCancelAllRequest$new("exchange_id_example") # OrderCancelAllRequest | OrderCancelAllRequest object.
#'
#' #Cancel all orders request
#' api_instance <- OrdersApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$V1OrdersCancelAllPost(var_order_cancel_all_requestdata_file = "result.txt")
#' result <- api_instance$V1OrdersCancelAllPost(var_order_cancel_all_request)
#' dput(result)
#'
#'
#' ####################  V1OrdersCancelPost  ####################
#'
#' library(openapi)
#' var_order_cancel_single_request <- OrderCancelSingleRequest$new("exchange_id_example", "exchange_order_id_example", "client_order_id_example") # OrderCancelSingleRequest | OrderCancelSingleRequest object.
#'
#' #Cancel order request
#' api_instance <- OrdersApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$V1OrdersCancelPost(var_order_cancel_single_requestdata_file = "result.txt")
#' result <- api_instance$V1OrdersCancelPost(var_order_cancel_single_request)
#' dput(result)
#'
#'
#' ####################  V1OrdersGet  ####################
#'
#' library(openapi)
#' var_exchange_id <- "KRAKEN" # character | Filter the open orders to the specific exchange. (Optional)
#'
#' #Get open orders
#' api_instance <- OrdersApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$V1OrdersGet(exchange_id = var_exchange_iddata_file = "result.txt")
#' result <- api_instance$V1OrdersGet(exchange_id = var_exchange_id)
#' dput(result)
#'
#'
#' ####################  V1OrdersHistoryTimeStartTimeEndGet  ####################
#'
#' library(openapi)
#' var_time_start <- "2022-05-01T00:00:00" # character | Start date
#' var_time_end <- "2022-05-01T12:00:00" # character | End date
#'
#' #History of order changes
#' api_instance <- OrdersApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$V1OrdersHistoryTimeStartTimeEndGet(var_time_start, var_time_enddata_file = "result.txt")
#' result <- api_instance$V1OrdersHistoryTimeStartTimeEndGet(var_time_start, var_time_end)
#' dput(result)
#'
#'
#' ####################  V1OrdersPost  ####################
#'
#' library(openapi)
#' var_order_new_single_request <- OrderNewSingleRequest$new("exchange_id_example", "client_order_id_example", 123, 123, OrdSide$new(), OrdType$new(), TimeInForce$new(), "symbol_id_exchange_example", "symbol_id_coinapi_example", "expire_time_example", c("MAKER_OR_CANCEL")) # OrderNewSingleRequest | OrderNewSingleRequest object.
#'
#' #Send new order
#' api_instance <- OrdersApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$V1OrdersPost(var_order_new_single_requestdata_file = "result.txt")
#' result <- api_instance$V1OrdersPost(var_order_new_single_request)
#' dput(result)
#'
#'
#' ####################  V1OrdersStatusClientOrderIdGet  ####################
#'
#' library(openapi)
#' var_client_order_id <- "6ab36bc1-344d-432e-ac6d-0bf44ee64c2b" # character | The unique identifier of the order assigned by the client.
#'
#' #Get order execution report
#' api_instance <- OrdersApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$V1OrdersStatusClientOrderIdGet(var_client_order_iddata_file = "result.txt")
#' result <- api_instance$V1OrdersStatusClientOrderIdGet(var_client_order_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
OrdersApi <- R6::R6Class(
  "OrdersApi",
  public = list(
    api_client = NULL,
    #' Initialize a new OrdersApi.
    #'
    #' @description
    #' Initialize a new OrdersApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Cancel all orders request
    #'
    #' @description
    #' Cancel all orders request
    #'
    #' @param order_cancel_all_request OrderCancelAllRequest object.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return MessageReject
    #' @export
    V1OrdersCancelAllPost = function(order_cancel_all_request, data_file = NULL, ...) {
      local_var_response <- self$V1OrdersCancelAllPostWithHttpInfo(order_cancel_all_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Cancel all orders request
    #'
    #' @description
    #' Cancel all orders request
    #'
    #' @param order_cancel_all_request OrderCancelAllRequest object.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (MessageReject) with additional information such as HTTP status code, headers
    #' @export
    V1OrdersCancelAllPostWithHttpInfo = function(order_cancel_all_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`order_cancel_all_request`)) {
        stop("Missing required parameter `order_cancel_all_request`.")
      }


      if (!is.null(`order_cancel_all_request`)) {
        local_var_body <- `order_cancel_all_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/orders/cancel/all"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "appliction/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "MessageReject", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Cancel order request
    #'
    #' @description
    #' Cancel order request
    #'
    #' @param order_cancel_single_request OrderCancelSingleRequest object.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrderExecutionReport
    #' @export
    V1OrdersCancelPost = function(order_cancel_single_request, data_file = NULL, ...) {
      local_var_response <- self$V1OrdersCancelPostWithHttpInfo(order_cancel_single_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Cancel order request
    #'
    #' @description
    #' Cancel order request
    #'
    #' @param order_cancel_single_request OrderCancelSingleRequest object.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrderExecutionReport) with additional information such as HTTP status code, headers
    #' @export
    V1OrdersCancelPostWithHttpInfo = function(order_cancel_single_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`order_cancel_single_request`)) {
        stop("Missing required parameter `order_cancel_single_request`.")
      }


      if (!is.null(`order_cancel_single_request`)) {
        local_var_body <- `order_cancel_single_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/orders/cancel"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "appliction/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "OrderExecutionReport", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get open orders
    #'
    #' @description
    #' Get open orders
    #'
    #' @param exchange_id (optional) Filter the open orders to the specific exchange.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[OrderExecutionReport]
    #' @export
    V1OrdersGet = function(exchange_id = NULL, data_file = NULL, ...) {
      local_var_response <- self$V1OrdersGetWithHttpInfo(exchange_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get open orders
    #'
    #' @description
    #' Get open orders
    #'
    #' @param exchange_id (optional) Filter the open orders to the specific exchange.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[OrderExecutionReport]) with additional information such as HTTP status code, headers
    #' @export
    V1OrdersGetWithHttpInfo = function(exchange_id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["exchange_id"]] <- `exchange_id`

      local_var_url_path <- "/v1/orders"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "appliction/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[OrderExecutionReport]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' History of order changes
    #'
    #' @description
    #' History of order changes
    #'
    #' @param time_start Start date
    #' @param time_end End date
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[OrderHistory]
    #' @export
    V1OrdersHistoryTimeStartTimeEndGet = function(time_start, time_end, data_file = NULL, ...) {
      local_var_response <- self$V1OrdersHistoryTimeStartTimeEndGetWithHttpInfo(time_start, time_end, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' History of order changes
    #'
    #' @description
    #' History of order changes
    #'
    #' @param time_start Start date
    #' @param time_end End date
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[OrderHistory]) with additional information such as HTTP status code, headers
    #' @export
    V1OrdersHistoryTimeStartTimeEndGetWithHttpInfo = function(time_start, time_end, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`time_start`)) {
        stop("Missing required parameter `time_start`.")
      }

      if (missing(`time_end`)) {
        stop("Missing required parameter `time_end`.")
      }



      local_var_url_path <- "/v1/orders/history/{time_start}/{time_end}"
      if (!missing(`time_start`)) {
        local_var_url_path <- gsub("\\{time_start\\}", URLencode(as.character(`time_start`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`time_end`)) {
        local_var_url_path <- gsub("\\{time_end\\}", URLencode(as.character(`time_end`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[OrderHistory]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Send new order
    #'
    #' @description
    #' Send new order
    #'
    #' @param order_new_single_request OrderNewSingleRequest object.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrderExecutionReport
    #' @export
    V1OrdersPost = function(order_new_single_request, data_file = NULL, ...) {
      local_var_response <- self$V1OrdersPostWithHttpInfo(order_new_single_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Send new order
    #'
    #' @description
    #' Send new order
    #'
    #' @param order_new_single_request OrderNewSingleRequest object.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrderExecutionReport) with additional information such as HTTP status code, headers
    #' @export
    V1OrdersPostWithHttpInfo = function(order_new_single_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`order_new_single_request`)) {
        stop("Missing required parameter `order_new_single_request`.")
      }


      if (!is.null(`order_new_single_request`)) {
        local_var_body <- `order_new_single_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/v1/orders"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "appliction/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "OrderExecutionReport", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get order execution report
    #'
    #' @description
    #' Get order execution report
    #'
    #' @param client_order_id The unique identifier of the order assigned by the client.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrderExecutionReport
    #' @export
    V1OrdersStatusClientOrderIdGet = function(client_order_id, data_file = NULL, ...) {
      local_var_response <- self$V1OrdersStatusClientOrderIdGetWithHttpInfo(client_order_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get order execution report
    #'
    #' @description
    #' Get order execution report
    #'
    #' @param client_order_id The unique identifier of the order assigned by the client.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrderExecutionReport) with additional information such as HTTP status code, headers
    #' @export
    V1OrdersStatusClientOrderIdGetWithHttpInfo = function(client_order_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`client_order_id`)) {
        stop("Missing required parameter `client_order_id`.")
      }


      local_var_url_path <- "/v1/orders/status/{client_order_id}"
      if (!missing(`client_order_id`)) {
        local_var_url_path <- gsub("\\{client_order_id\\}", URLencode(as.character(`client_order_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "OrderExecutionReport", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
