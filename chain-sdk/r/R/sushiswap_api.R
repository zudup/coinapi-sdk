#' On Chain Dapps - REST API
#'
#'  This section will provide necessary information about the `OnChain API` protocol.  <br/><br/> Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged. It was popularised in the 1960s with the release of Letraset sheets containing Lorem Ipsum passages, and more recently with desktop publishing software like Aldus PageMaker including versions of Lorem Ipsum.                             
#'
#' The version of the OpenAPI document: v1
#' Contact: support@coinapi.io
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Sushiswap operations
#' @description SushiswapApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ SushiswapBundlesCurrent } \emph{ Bundles (current) }
#' Gets bundles.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.BundleDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapBundleDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapBurnsCurrent } \emph{ Burns (current) }
#' Gets burns.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.BurnDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapBurnDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapDayDataCurrent } \emph{ DayData (current) }
#' Gets dayData.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.DayDataDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapDayDataDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapFactoriesCurrent } \emph{ Factories (current) }
#' Gets factories.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.FactoryDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapFactoryDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetBundlesHistorical } \emph{ Bundles (historical) }
#' Gets bundles.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Sushiswap.BundleDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapBundleDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetBurnsHistorical } \emph{ Burns (historical) }
#' Gets burns.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pair character
#' \item \emph{ @returnType } list( \link{Sushiswap.BurnDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapBurnDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetDayDataHistorical } \emph{ DayData (historical) }
#' Gets dayData.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Sushiswap.DayDataDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapDayDataDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetFactoriesHistorical } \emph{ Factories (historical) }
#' Gets factories.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Sushiswap.FactoryDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapFactoryDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetHourDataHistorical } \emph{ HourData (historical) }
#' Gets hourData.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Sushiswap.HourDataDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapHourDataDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetLiquidityPositionSnapshotsHistorical } \emph{ LiquidityPositionSnapshots (historical) }
#' Gets liquidityPositionSnapshots.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } user character
#' \item \emph{ @param } pair character
#' \item \emph{ @returnType } list( \link{Sushiswap.LiquidityPositionSnapshotDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapLiquidityPositionSnapshotDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetLiquidityPositionsHistorical } \emph{ LiquidityPositions (historical) }
#' Gets liquidityPositions.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } user character
#' \item \emph{ @param } pair character
#' \item \emph{ @returnType } list( \link{Sushiswap.LiquidityPositionDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapLiquidityPositionDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetMintsHistorical } \emph{ Mints (historical) }
#' Gets mints.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pair character
#' \item \emph{ @returnType } list( \link{Sushiswap.MintDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapMintDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetPairDayDataHistorical } \emph{ PairDayData (historical) }
#' Gets pairDayData.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pair character
#' \item \emph{ @param } token_0 character
#' \item \emph{ @param } token_1 character
#' \item \emph{ @returnType } list( \link{Sushiswap.PairDayDataDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapPairDayDataDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetPairHourDataHistorical } \emph{ PairHourData (historical) }
#' Gets pairHourData.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pair character
#' \item \emph{ @returnType } list( \link{Sushiswap.PairHourDataDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapPairHourDataDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetPairsHistorical } \emph{ Pairs (historical) }
#' Gets pairs.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } name character
#' \item \emph{ @param } token_0 character
#' \item \emph{ @param } token_1 character
#' \item \emph{ @returnType } list( \link{Sushiswap.PairDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapPairDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetSwapsHistorical } \emph{ Swaps (historical) }
#' Gets swaps.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pair character
#' \item \emph{ @returnType } list( \link{Sushiswap.SwapDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapSwapDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetTokenDayDataHistorical } \emph{ TokenDayData (historical) }
#' Gets tokenDayData.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Sushiswap.TokenDayDataDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapTokenDayDataDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetTokensHistorical } \emph{ Tokens (historical) }
#' Gets tokens.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } symbol character
#' \item \emph{ @param } name character
#' \item \emph{ @returnType } list( \link{Sushiswap.TokenDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapTokenDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetTransactionsHistorical } \emph{ Transactions (historical) }
#' Gets transactions.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Sushiswap.TransactionDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapTransactionDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapGetUsersHistorical } \emph{ Users (historical) }
#' Gets users.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Sushiswap.UserDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapUserDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapHourDataCurrent } \emph{ HourData (current) }
#' Gets hourData.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.HourDataDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapHourDataDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapLiquidityPositionSnapshotsCurrent } \emph{ LiquidityPositionSnapshots (current) }
#' Gets liquidityPositionSnapshots.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.LiquidityPositionSnapshotDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapLiquidityPositionSnapshotDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapLiquidityPositionsCurrent } \emph{ LiquidityPositions (current) }
#' Gets liquidityPositions.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.LiquidityPositionDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapLiquidityPositionDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapMintsCurrent } \emph{ Mints (current) }
#' Gets mints.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.MintDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapMintDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapPairDayDataCurrent } \emph{ PairDayData (current) }
#' Gets pairDayData.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.PairDayDataDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapPairDayDataDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapPairHourDataCurrent } \emph{ PairHourData (current) }
#' Gets pairHourData.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.PairHourDataDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapPairHourDataDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapPairsCurrent } \emph{ Pairs (current) }
#' Gets pairs.
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Sushiswap.PairDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapPairDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapSwapsCurrent } \emph{ Swaps (current) }
#' Gets swaps.
#'
#' \itemize{
#' \item \emph{ @param } pair character
#' \item \emph{ @returnType } list( \link{Sushiswap.SwapDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapSwapDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapTokenDayDataCurrent } \emph{ TokenDayData (current) }
#' Gets tokenDayData.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.TokenDayDataDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapTokenDayDataDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapTokensCurrent } \emph{ Tokens (current) }
#' Gets tokens.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.TokenDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapTokenDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapTransactionsCurrent } \emph{ Transactions (current) }
#' Gets transactions.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.TransactionDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapTransactionDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SushiswapUsersCurrent } \emph{ Users (current) }
#' Gets users.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Sushiswap.UserDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[SushiswapUserDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  SushiswapBundlesCurrent  ####################
#'
#' library(openapi)
#'
#' #Bundles (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapBundlesCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapBundlesCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapBurnsCurrent  ####################
#'
#' library(openapi)
#'
#' #Burns (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapBurnsCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapBurnsCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapDayDataCurrent  ####################
#'
#' library(openapi)
#'
#' #DayData (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapDayDataCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapDayDataCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapFactoriesCurrent  ####################
#'
#' library(openapi)
#'
#' #Factories (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapFactoriesCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapFactoriesCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapGetBundlesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Hardcoded to '1'. (Optional)
#'
#' #Bundles (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetBundlesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$SushiswapGetBundlesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  SushiswapGetBurnsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer |  (Optional)
#' var_end_block <- 56 # integer |  (Optional)
#' var_start_date <- "start_date_example" # character |  (Optional)
#' var_end_date <- "end_date_example" # character |  (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pair <- "pair_example" # character |  (Optional)
#'
#' #Burns (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetBurnsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pair = var_pairdata_file = "result.txt")
#' result <- api_instance$SushiswapGetBurnsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pair = var_pair)
#' dput(result)
#'
#'
#' ####################  SushiswapGetDayDataHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Unix timestamp for start of day / 86400 giving a unique day index. (Optional)
#'
#' #DayData (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetDayDataHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$SushiswapGetDayDataHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  SushiswapGetFactoriesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Factory address. (Optional)
#'
#' #Factories (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetFactoriesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$SushiswapGetFactoriesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  SushiswapGetHourDataHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Start of hour timestamp. (Optional)
#'
#' #HourData (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetHourDataHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$SushiswapGetHourDataHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  SushiswapGetLiquidityPositionSnapshotsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer |  (Optional)
#' var_end_block <- 56 # integer |  (Optional)
#' var_start_date <- "start_date_example" # character |  (Optional)
#' var_end_date <- "end_date_example" # character |  (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_user <- "user_example" # character |  (Optional)
#' var_pair <- "pair_example" # character |  (Optional)
#'
#' #LiquidityPositionSnapshots (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetLiquidityPositionSnapshotsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, user = var_user, pair = var_pairdata_file = "result.txt")
#' result <- api_instance$SushiswapGetLiquidityPositionSnapshotsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, user = var_user, pair = var_pair)
#' dput(result)
#'
#'
#' ####################  SushiswapGetLiquidityPositionsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer |  (Optional)
#' var_end_block <- 56 # integer |  (Optional)
#' var_start_date <- "start_date_example" # character |  (Optional)
#' var_end_date <- "end_date_example" # character |  (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_user <- "user_example" # character |  (Optional)
#' var_pair <- "pair_example" # character |  (Optional)
#'
#' #LiquidityPositions (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetLiquidityPositionsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, user = var_user, pair = var_pairdata_file = "result.txt")
#' result <- api_instance$SushiswapGetLiquidityPositionsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, user = var_user, pair = var_pair)
#' dput(result)
#'
#'
#' ####################  SushiswapGetMintsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer |  (Optional)
#' var_end_block <- 56 # integer |  (Optional)
#' var_start_date <- "start_date_example" # character |  (Optional)
#' var_end_date <- "end_date_example" # character |  (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pair <- "pair_example" # character |  (Optional)
#'
#' #Mints (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetMintsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pair = var_pairdata_file = "result.txt")
#' result <- api_instance$SushiswapGetMintsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pair = var_pair)
#' dput(result)
#'
#'
#' ####################  SushiswapGetPairDayDataHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer |  (Optional)
#' var_end_block <- 56 # integer |  (Optional)
#' var_start_date <- "start_date_example" # character |  (Optional)
#' var_end_date <- "end_date_example" # character |  (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pair <- "pair_example" # character |  (Optional)
#' var_token_0 <- "token_0_example" # character |  (Optional)
#' var_token_1 <- "token_1_example" # character |  (Optional)
#'
#' #PairDayData (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetPairDayDataHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pair = var_pair, token_0 = var_token_0, token_1 = var_token_1data_file = "result.txt")
#' result <- api_instance$SushiswapGetPairDayDataHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pair = var_pair, token_0 = var_token_0, token_1 = var_token_1)
#' dput(result)
#'
#'
#' ####################  SushiswapGetPairHourDataHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer |  (Optional)
#' var_end_block <- 56 # integer |  (Optional)
#' var_start_date <- "start_date_example" # character |  (Optional)
#' var_end_date <- "end_date_example" # character |  (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pair <- "pair_example" # character |  (Optional)
#'
#' #PairHourData (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetPairHourDataHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pair = var_pairdata_file = "result.txt")
#' result <- api_instance$SushiswapGetPairHourDataHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pair = var_pair)
#' dput(result)
#'
#'
#' ####################  SushiswapGetPairsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer |  (Optional)
#' var_end_block <- 56 # integer |  (Optional)
#' var_start_date <- "start_date_example" # character |  (Optional)
#' var_end_date <- "end_date_example" # character |  (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_name <- "name_example" # character |  (Optional)
#' var_token_0 <- "token_0_example" # character |  (Optional)
#' var_token_1 <- "token_1_example" # character |  (Optional)
#'
#' #Pairs (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetPairsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, name = var_name, token_0 = var_token_0, token_1 = var_token_1data_file = "result.txt")
#' result <- api_instance$SushiswapGetPairsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, name = var_name, token_0 = var_token_0, token_1 = var_token_1)
#' dput(result)
#'
#'
#' ####################  SushiswapGetSwapsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Transaction hash plus index in Transaction swap array. (Optional)
#' var_pair <- "pair_example" # character | Reference to pair. (Optional)
#'
#' #Swaps (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetSwapsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pair = var_pairdata_file = "result.txt")
#' result <- api_instance$SushiswapGetSwapsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pair = var_pair)
#' dput(result)
#'
#'
#' ####################  SushiswapGetTokenDayDataHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Identifier, day start timestamp in unix / 86400. (Optional)
#'
#' #TokenDayData (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetTokenDayDataHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$SushiswapGetTokenDayDataHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  SushiswapGetTokensHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Token address. (Optional)
#' var_symbol <- "symbol_example" # character | Token symbol. (Optional)
#' var_name <- "name_example" # character | Token name. (Optional)
#'
#' #Tokens (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetTokensHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, symbol = var_symbol, name = var_namedata_file = "result.txt")
#' result <- api_instance$SushiswapGetTokensHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, symbol = var_symbol, name = var_name)
#' dput(result)
#'
#'
#' ####################  SushiswapGetTransactionsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Ethereum transaction hash. (Optional)
#'
#' #Transactions (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetTransactionsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$SushiswapGetTransactionsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  SushiswapGetUsersHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | User address. (Optional)
#'
#' #Users (historical)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapGetUsersHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$SushiswapGetUsersHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  SushiswapHourDataCurrent  ####################
#'
#' library(openapi)
#'
#' #HourData (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapHourDataCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapHourDataCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapLiquidityPositionSnapshotsCurrent  ####################
#'
#' library(openapi)
#'
#' #LiquidityPositionSnapshots (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapLiquidityPositionSnapshotsCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapLiquidityPositionSnapshotsCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapLiquidityPositionsCurrent  ####################
#'
#' library(openapi)
#'
#' #LiquidityPositions (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapLiquidityPositionsCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapLiquidityPositionsCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapMintsCurrent  ####################
#'
#' library(openapi)
#'
#' #Mints (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapMintsCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapMintsCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapPairDayDataCurrent  ####################
#'
#' library(openapi)
#'
#' #PairDayData (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapPairDayDataCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapPairDayDataCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapPairHourDataCurrent  ####################
#'
#' library(openapi)
#'
#' #PairHourData (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapPairHourDataCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapPairHourDataCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapPairsCurrent  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | Pair contract address. (Optional)
#'
#' #Pairs (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapPairsCurrent(id = var_iddata_file = "result.txt")
#' result <- api_instance$SushiswapPairsCurrent(id = var_id)
#' dput(result)
#'
#'
#' ####################  SushiswapSwapsCurrent  ####################
#'
#' library(openapi)
#' var_pair <- "pair_example" # character | Reference to pair. (Optional)
#'
#' #Swaps (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapSwapsCurrent(pair = var_pairdata_file = "result.txt")
#' result <- api_instance$SushiswapSwapsCurrent(pair = var_pair)
#' dput(result)
#'
#'
#' ####################  SushiswapTokenDayDataCurrent  ####################
#'
#' library(openapi)
#'
#' #TokenDayData (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapTokenDayDataCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapTokenDayDataCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapTokensCurrent  ####################
#'
#' library(openapi)
#'
#' #Tokens (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapTokensCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapTokensCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapTransactionsCurrent  ####################
#'
#' library(openapi)
#'
#' #Transactions (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapTransactionsCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapTransactionsCurrent()
#' dput(result)
#'
#'
#' ####################  SushiswapUsersCurrent  ####################
#'
#' library(openapi)
#'
#' #Users (current)
#' api_instance <- SushiswapApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SushiswapUsersCurrent(data_file = "result.txt")
#' result <- api_instance$SushiswapUsersCurrent()
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
SushiswapApi <- R6::R6Class(
  "SushiswapApi",
  public = list(
    api_client = NULL,
    #' Initialize a new SushiswapApi.
    #'
    #' @description
    #' Initialize a new SushiswapApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Bundles (current)
    #'
    #' @description
    #' Bundles (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapBundleDTO]
    #' @export
    SushiswapBundlesCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapBundlesCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Bundles (current)
    #'
    #' @description
    #' Bundles (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapBundleDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapBundlesCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/bundles/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapBundleDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Burns (current)
    #'
    #' @description
    #' Burns (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapBurnDTO]
    #' @export
    SushiswapBurnsCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapBurnsCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Burns (current)
    #'
    #' @description
    #' Burns (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapBurnDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapBurnsCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/burns/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapBurnDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' DayData (current)
    #'
    #' @description
    #' DayData (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapDayDataDTO]
    #' @export
    SushiswapDayDataCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapDayDataCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' DayData (current)
    #'
    #' @description
    #' DayData (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapDayDataDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapDayDataCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/dayData/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapDayDataDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Factories (current)
    #'
    #' @description
    #' Factories (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapFactoryDTO]
    #' @export
    SushiswapFactoriesCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapFactoriesCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Factories (current)
    #'
    #' @description
    #' Factories (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapFactoryDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapFactoriesCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/factories/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapFactoryDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Bundles (historical)
    #'
    #' @description
    #' Bundles (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Hardcoded to '1'.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapBundleDTO]
    #' @export
    SushiswapGetBundlesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetBundlesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Bundles (historical)
    #'
    #' @description
    #' Bundles (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Hardcoded to '1'.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapBundleDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetBundlesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/sushiswap/bundles/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapBundleDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Burns (historical)
    #'
    #' @description
    #' Burns (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pair (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapBurnDTO]
    #' @export
    SushiswapGetBurnsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pair = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetBurnsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pair, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Burns (historical)
    #'
    #' @description
    #' Burns (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pair (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapBurnDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetBurnsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pair = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pair"]] <- `pair`

      local_var_url_path <- "/dapps/sushiswap/burns/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapBurnDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' DayData (historical)
    #'
    #' @description
    #' DayData (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Unix timestamp for start of day / 86400 giving a unique day index.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapDayDataDTO]
    #' @export
    SushiswapGetDayDataHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetDayDataHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' DayData (historical)
    #'
    #' @description
    #' DayData (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Unix timestamp for start of day / 86400 giving a unique day index.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapDayDataDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetDayDataHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/sushiswap/dayData/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapDayDataDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Factories (historical)
    #'
    #' @description
    #' Factories (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Factory address.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapFactoryDTO]
    #' @export
    SushiswapGetFactoriesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetFactoriesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Factories (historical)
    #'
    #' @description
    #' Factories (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Factory address.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapFactoryDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetFactoriesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/sushiswap/factories/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapFactoryDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' HourData (historical)
    #'
    #' @description
    #' HourData (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Start of hour timestamp.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapHourDataDTO]
    #' @export
    SushiswapGetHourDataHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetHourDataHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' HourData (historical)
    #'
    #' @description
    #' HourData (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Start of hour timestamp.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapHourDataDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetHourDataHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/sushiswap/hourData/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapHourDataDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' LiquidityPositionSnapshots (historical)
    #'
    #' @description
    #' LiquidityPositionSnapshots (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param user (optional) No description
    #' @param pair (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapLiquidityPositionSnapshotDTO]
    #' @export
    SushiswapGetLiquidityPositionSnapshotsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, user = NULL, pair = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetLiquidityPositionSnapshotsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, user, pair, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' LiquidityPositionSnapshots (historical)
    #'
    #' @description
    #' LiquidityPositionSnapshots (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param user (optional) No description
    #' @param pair (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapLiquidityPositionSnapshotDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetLiquidityPositionSnapshotsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, user = NULL, pair = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE








      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["user"]] <- `user`

      query_params[["pair"]] <- `pair`

      local_var_url_path <- "/dapps/sushiswap/liquidityPositionSnapshots/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapLiquidityPositionSnapshotDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' LiquidityPositions (historical)
    #'
    #' @description
    #' LiquidityPositions (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param user (optional) No description
    #' @param pair (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapLiquidityPositionDTO]
    #' @export
    SushiswapGetLiquidityPositionsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, user = NULL, pair = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetLiquidityPositionsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, user, pair, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' LiquidityPositions (historical)
    #'
    #' @description
    #' LiquidityPositions (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param user (optional) No description
    #' @param pair (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapLiquidityPositionDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetLiquidityPositionsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, user = NULL, pair = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE








      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["user"]] <- `user`

      query_params[["pair"]] <- `pair`

      local_var_url_path <- "/dapps/sushiswap/liquidityPositions/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapLiquidityPositionDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Mints (historical)
    #'
    #' @description
    #' Mints (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pair (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapMintDTO]
    #' @export
    SushiswapGetMintsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pair = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetMintsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pair, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Mints (historical)
    #'
    #' @description
    #' Mints (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pair (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapMintDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetMintsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pair = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pair"]] <- `pair`

      local_var_url_path <- "/dapps/sushiswap/mints/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapMintDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' PairDayData (historical)
    #'
    #' @description
    #' PairDayData (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pair (optional) No description
    #' @param token_0 (optional) No description
    #' @param token_1 (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapPairDayDataDTO]
    #' @export
    SushiswapGetPairDayDataHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pair = NULL, token_0 = NULL, token_1 = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetPairDayDataHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pair, token_0, token_1, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' PairDayData (historical)
    #'
    #' @description
    #' PairDayData (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pair (optional) No description
    #' @param token_0 (optional) No description
    #' @param token_1 (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapPairDayDataDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetPairDayDataHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pair = NULL, token_0 = NULL, token_1 = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE









      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pair"]] <- `pair`

      query_params[["token_0"]] <- `token_0`

      query_params[["token_1"]] <- `token_1`

      local_var_url_path <- "/dapps/sushiswap/pairDayData/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapPairDayDataDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' PairHourData (historical)
    #'
    #' @description
    #' PairHourData (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pair (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapPairHourDataDTO]
    #' @export
    SushiswapGetPairHourDataHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pair = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetPairHourDataHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pair, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' PairHourData (historical)
    #'
    #' @description
    #' PairHourData (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pair (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapPairHourDataDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetPairHourDataHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pair = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pair"]] <- `pair`

      local_var_url_path <- "/dapps/sushiswap/pairHourData/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapPairHourDataDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Pairs (historical)
    #'
    #' @description
    #' Pairs (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param name (optional) No description
    #' @param token_0 (optional) No description
    #' @param token_1 (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapPairDTO]
    #' @export
    SushiswapGetPairsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, name = NULL, token_0 = NULL, token_1 = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetPairsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, name, token_0, token_1, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Pairs (historical)
    #'
    #' @description
    #' Pairs (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param name (optional) No description
    #' @param token_0 (optional) No description
    #' @param token_1 (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapPairDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetPairsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, name = NULL, token_0 = NULL, token_1 = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE









      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["name"]] <- `name`

      query_params[["token_0"]] <- `token_0`

      query_params[["token_1"]] <- `token_1`

      local_var_url_path <- "/dapps/sushiswap/pairs/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapPairDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Swaps (historical)
    #'
    #' @description
    #' Swaps (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Transaction hash plus index in Transaction swap array.
    #' @param pair (optional) Reference to pair.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapSwapDTO]
    #' @export
    SushiswapGetSwapsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pair = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetSwapsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pair, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Swaps (historical)
    #'
    #' @description
    #' Swaps (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Transaction hash plus index in Transaction swap array.
    #' @param pair (optional) Reference to pair.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapSwapDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetSwapsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pair = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pair"]] <- `pair`

      local_var_url_path <- "/dapps/sushiswap/swaps/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapSwapDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' TokenDayData (historical)
    #'
    #' @description
    #' TokenDayData (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Identifier, day start timestamp in unix / 86400.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapTokenDayDataDTO]
    #' @export
    SushiswapGetTokenDayDataHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetTokenDayDataHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' TokenDayData (historical)
    #'
    #' @description
    #' TokenDayData (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Identifier, day start timestamp in unix / 86400.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapTokenDayDataDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetTokenDayDataHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/sushiswap/tokenDayData/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapTokenDayDataDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Tokens (historical)
    #'
    #' @description
    #' Tokens (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Token address.
    #' @param symbol (optional) Token symbol.
    #' @param name (optional) Token name.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapTokenDTO]
    #' @export
    SushiswapGetTokensHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, symbol = NULL, name = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetTokensHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, symbol, name, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Tokens (historical)
    #'
    #' @description
    #' Tokens (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Token address.
    #' @param symbol (optional) Token symbol.
    #' @param name (optional) Token name.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapTokenDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetTokensHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, symbol = NULL, name = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE








      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["symbol"]] <- `symbol`

      query_params[["name"]] <- `name`

      local_var_url_path <- "/dapps/sushiswap/tokens/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapTokenDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Transactions (historical)
    #'
    #' @description
    #' Transactions (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Ethereum transaction hash.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapTransactionDTO]
    #' @export
    SushiswapGetTransactionsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetTransactionsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Transactions (historical)
    #'
    #' @description
    #' Transactions (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Ethereum transaction hash.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapTransactionDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetTransactionsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/sushiswap/transactions/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapTransactionDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Users (historical)
    #'
    #' @description
    #' Users (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) User address.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapUserDTO]
    #' @export
    SushiswapGetUsersHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapGetUsersHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Users (historical)
    #'
    #' @description
    #' Users (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) User address.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapUserDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapGetUsersHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/sushiswap/users/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapUserDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' HourData (current)
    #'
    #' @description
    #' HourData (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapHourDataDTO]
    #' @export
    SushiswapHourDataCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapHourDataCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' HourData (current)
    #'
    #' @description
    #' HourData (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapHourDataDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapHourDataCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/hourData/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapHourDataDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' LiquidityPositionSnapshots (current)
    #'
    #' @description
    #' LiquidityPositionSnapshots (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapLiquidityPositionSnapshotDTO]
    #' @export
    SushiswapLiquidityPositionSnapshotsCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapLiquidityPositionSnapshotsCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' LiquidityPositionSnapshots (current)
    #'
    #' @description
    #' LiquidityPositionSnapshots (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapLiquidityPositionSnapshotDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapLiquidityPositionSnapshotsCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/liquidityPositionSnapshots/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapLiquidityPositionSnapshotDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' LiquidityPositions (current)
    #'
    #' @description
    #' LiquidityPositions (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapLiquidityPositionDTO]
    #' @export
    SushiswapLiquidityPositionsCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapLiquidityPositionsCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' LiquidityPositions (current)
    #'
    #' @description
    #' LiquidityPositions (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapLiquidityPositionDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapLiquidityPositionsCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/liquidityPositions/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapLiquidityPositionDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Mints (current)
    #'
    #' @description
    #' Mints (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapMintDTO]
    #' @export
    SushiswapMintsCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapMintsCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Mints (current)
    #'
    #' @description
    #' Mints (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapMintDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapMintsCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/mints/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapMintDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' PairDayData (current)
    #'
    #' @description
    #' PairDayData (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapPairDayDataDTO]
    #' @export
    SushiswapPairDayDataCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapPairDayDataCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' PairDayData (current)
    #'
    #' @description
    #' PairDayData (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapPairDayDataDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapPairDayDataCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/pairDayData/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapPairDayDataDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' PairHourData (current)
    #'
    #' @description
    #' PairHourData (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapPairHourDataDTO]
    #' @export
    SushiswapPairHourDataCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapPairHourDataCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' PairHourData (current)
    #'
    #' @description
    #' PairHourData (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapPairHourDataDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapPairHourDataCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/pairHourData/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapPairHourDataDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Pairs (current)
    #'
    #' @description
    #' Pairs (current)
    #'
    #' @param id (optional) Pair contract address.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapPairDTO]
    #' @export
    SushiswapPairsCurrent = function(id = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapPairsCurrentWithHttpInfo(id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Pairs (current)
    #'
    #' @description
    #' Pairs (current)
    #'
    #' @param id (optional) Pair contract address.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapPairDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapPairsCurrentWithHttpInfo = function(id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/sushiswap/pairs/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapPairDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Swaps (current)
    #'
    #' @description
    #' Swaps (current)
    #'
    #' @param pair (optional) Reference to pair.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapSwapDTO]
    #' @export
    SushiswapSwapsCurrent = function(pair = NULL, data_file = NULL, ...) {
      local_var_response <- self$SushiswapSwapsCurrentWithHttpInfo(pair, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Swaps (current)
    #'
    #' @description
    #' Swaps (current)
    #'
    #' @param pair (optional) Reference to pair.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapSwapDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapSwapsCurrentWithHttpInfo = function(pair = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["pair"]] <- `pair`

      local_var_url_path <- "/dapps/sushiswap/swaps/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapSwapDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' TokenDayData (current)
    #'
    #' @description
    #' TokenDayData (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapTokenDayDataDTO]
    #' @export
    SushiswapTokenDayDataCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapTokenDayDataCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' TokenDayData (current)
    #'
    #' @description
    #' TokenDayData (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapTokenDayDataDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapTokenDayDataCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/tokenDayData/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapTokenDayDataDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Tokens (current)
    #'
    #' @description
    #' Tokens (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapTokenDTO]
    #' @export
    SushiswapTokensCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapTokensCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Tokens (current)
    #'
    #' @description
    #' Tokens (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapTokenDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapTokensCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/tokens/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapTokenDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Transactions (current)
    #'
    #' @description
    #' Transactions (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapTransactionDTO]
    #' @export
    SushiswapTransactionsCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapTransactionsCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Transactions (current)
    #'
    #' @description
    #' Transactions (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapTransactionDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapTransactionsCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/transactions/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapTransactionDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Users (current)
    #'
    #' @description
    #' Users (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SushiswapUserDTO]
    #' @export
    SushiswapUsersCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$SushiswapUsersCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Users (current)
    #'
    #' @description
    #' Users (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SushiswapUserDTO]) with additional information such as HTTP status code, headers
    #' @export
    SushiswapUsersCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/sushiswap/users/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[SushiswapUserDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
