#' OnChain API
#'
#'  This section will provide necessary information about the `OnChain API` protocol.  <br/><br/> Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged. It was popularised in the 1960s with the release of Letraset sheets containing Lorem Ipsum passages, and more recently with desktop publishing software like Aldus PageMaker including versions of Lorem Ipsum.                             
#'
#' The version of the OpenAPI document: v1
#' Contact: support@coinapi.io
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Curve operations
#' @description CurveApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ CurveGetAccountsHistorical } \emph{ Accounts (historical) 🔥 }
#' Gets accounts.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } address character
#' \item \emph{ @returnType } list( \link{Curve.AccountDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveAccountDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetAddLiquidityEventsHistorical } \emph{ AddLiquidityEvents (historical) 🔥 }
#' Gets addliquidityevents.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.AddLiquidityEventDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveAddLiquidityEventDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetAdminFeeChangeLogsHistorical } \emph{ AdminFeeChangeLogs (historical) 🔥 }
#' Gets adminfeechangelogs.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.AdminFeeChangeLogDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveAdminFeeChangeLogDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetAmplificationCoeffChangeLogsHistorical } \emph{ AmplificationCoeffChangeLogs (historical) 🔥 }
#' Gets amplificationcoeffchangelogs.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.AmplificationCoeffChangeLogDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveAmplificationCoeffChangeLogDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetCoinsHistorical } \emph{ Coins (historical) 🔥 }
#' Gets coins.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.CoinDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveCoinDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetContractVersionsHistorical } \emph{ ContractVersions (historical) 🔥 }
#' Gets contractversions.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } address character
#' \item \emph{ @returnType } list( \link{Curve.ContractVersionDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveContractVersionDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetContractsHistorical } \emph{ Contracts (historical) 🔥 }
#' Gets contracts.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Curve.ContractDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveContractDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetDailyVolumesHistorical } \emph{ DailyVolumes (historical) 🔥 }
#' Gets dailyvolumes.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.DailyVolumeDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveDailyVolumeDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetExchangesHistorical } \emph{ Exchanges (historical) 🔥 }
#' Gets exchanges.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.ExchangeDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveExchangeDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetFeeChangeLogsHistorical } \emph{ FeeChangeLogs (historical) 🔥 }
#' Gets feechangelogs.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.FeeChangeLogDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveFeeChangeLogDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetGaugeDepositsHistorical } \emph{ GaugeDeposits (historical) 🔥 }
#' Gets gaugedeposits.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Curve.GaugeDepositDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveGaugeDepositDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetGaugeLiquiditysHistorical } \emph{ GaugeLiquiditys (historical) 🔥 }
#' Gets gaugeliquiditys.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } user character
#' \item \emph{ @returnType } list( \link{Curve.GaugeLiquidityDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveGaugeLiquidityDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetGaugeTotalWeightsHistorical } \emph{ GaugeTotalWeights (historical) 🔥 }
#' Gets gaugetotalweights.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Curve.GaugeTotalWeightDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveGaugeTotalWeightDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetGaugeTypeWeightsHistorical } \emph{ GaugeTypeWeights (historical) 🔥 }
#' Gets gaugetypeweights.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Curve.GaugeTypeWeightDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveGaugeTypeWeightDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetGaugeTypesHistorical } \emph{ GaugeTypes (historical) 🔥 }
#' Gets gaugetypes.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } name character
#' \item \emph{ @returnType } list( \link{Curve.GaugeTypeDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveGaugeTypeDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetGaugeWeightVotesHistorical } \emph{ GaugeWeightVotes (historical) 🔥 }
#' Gets gaugeweightvotes.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } user character
#' \item \emph{ @returnType } list( \link{Curve.GaugeWeightVoteDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveGaugeWeightVoteDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetGaugeWeightsHistorical } \emph{ GaugeWeights (historical) 🔥 }
#' Gets gaugeweights.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Curve.GaugeWeightDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveGaugeWeightDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetGaugeWithdrawsHistorical } \emph{ GaugeWithdraws (historical) 🔥 }
#' Gets gaugewithdraws.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Curve.GaugeWithdrawDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveGaugeWithdrawDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetGaugesHistorical } \emph{ Gauges (historical) 🔥 }
#' Gets gauges.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } address character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.GaugeDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveGaugeDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetHourlyVolumesHistorical } \emph{ HourlyVolumes (historical) 🔥 }
#' Gets hourlyvolumes.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.HourlyVolumeDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveHourlyVolumeDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetLpTokensHistorical } \emph{ LpTokens (historical) 🔥 }
#' Gets lptokens.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } address character
#' \item \emph{ @param } name character
#' \item \emph{ @param } symbol character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.LpTokenDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveLpTokenDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetPoolsHistorical } \emph{ Pools (historical) 🔥 }
#' Gets pools.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } name character
#' \item \emph{ @returnType } list( \link{Curve.PoolDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurvePoolDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetProposalVotesHistorical } \emph{ ProposalVotes (historical) 🔥 }
#' Gets proposalvotes.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Curve.ProposalVoteDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveProposalVoteDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetProposalsHistorical } \emph{ Proposals (historical) 🔥 }
#' Gets proposals.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Curve.ProposalDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveProposalDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetRemoveLiquidityEventsHistorical } \emph{ RemoveLiquidityEvents (historical) 🔥 }
#' Gets removeliquidityevents.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.RemoveLiquidityEventDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveRemoveLiquidityEventDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetRemoveLiquidityOneEventsHistorical } \emph{ RemoveLiquidityOneEvents (historical) 🔥 }
#' Gets removeliquidityoneevents.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.RemoveLiquidityOneEventDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveRemoveLiquidityOneEventDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetSystemStatesHistorical } \emph{ SystemStates (historical) 🔥 }
#' Gets systemstates.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Curve.SystemStateDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveSystemStateDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetTokensHistorical } \emph{ Tokens (historical) 🔥 }
#' Gets tokens.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } address character
#' \item \emph{ @param } name character
#' \item \emph{ @param } symbol character
#' \item \emph{ @returnType } list( \link{Curve.TokenDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveTokenDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetTransferOwnershipEventsHistorical } \emph{ TransferOwnershipEvents (historical) 🔥 }
#' Gets transferownershipevents.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.TransferOwnershipEventDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveTransferOwnershipEventDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetUnderlyingCoinsHistorical } \emph{ UnderlyingCoins (historical) 🔥 }
#' Gets underlyingcoins.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.UnderlyingCoinDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveUnderlyingCoinDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetVotingAppsHistorical } \emph{ VotingApps (historical) 🔥 }
#' Gets votingapps.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } address character
#' \item \emph{ @returnType } list( \link{Curve.VotingAppDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveVotingAppDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CurveGetWeeklyVolumesHistorical } \emph{ WeeklyVolumes (historical) 🔥 }
#' Gets weeklyvolumes.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } pool character
#' \item \emph{ @returnType } list( \link{Curve.WeeklyVolumeDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CurveWeeklyVolumeDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  CurveGetAccountsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_address <- "address_example" # character |  (Optional)
#'
#' #Accounts (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetAccountsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_addressdata_file = "result.txt")
#' result <- api_instance$CurveGetAccountsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address)
#' dput(result)
#'
#'
#' ####################  CurveGetAddLiquidityEventsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #AddLiquidityEvents (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetAddLiquidityEventsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetAddLiquidityEventsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetAdminFeeChangeLogsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #AdminFeeChangeLogs (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetAdminFeeChangeLogsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetAdminFeeChangeLogsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetAmplificationCoeffChangeLogsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #AmplificationCoeffChangeLogs (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetAmplificationCoeffChangeLogsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetAmplificationCoeffChangeLogsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetCoinsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer |  (Optional)
#' var_end_block <- 56 # integer |  (Optional)
#' var_start_date <- "start_date_example" # character |  (Optional)
#' var_end_date <- "end_date_example" # character |  (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #Coins (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetCoinsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetCoinsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetContractVersionsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_address <- "address_example" # character |  (Optional)
#'
#' #ContractVersions (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetContractVersionsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_addressdata_file = "result.txt")
#' result <- api_instance$CurveGetContractVersionsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address)
#' dput(result)
#'
#'
#' ####################  CurveGetContractsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#'
#' #Contracts (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetContractsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$CurveGetContractsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  CurveGetDailyVolumesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #DailyVolumes (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetDailyVolumesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetDailyVolumesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetExchangesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #Exchanges (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetExchangesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetExchangesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetFeeChangeLogsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #FeeChangeLogs (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetFeeChangeLogsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetFeeChangeLogsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetGaugeDepositsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#'
#' #GaugeDeposits (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetGaugeDepositsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$CurveGetGaugeDepositsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  CurveGetGaugeLiquiditysHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_user <- "user_example" # character |  (Optional)
#'
#' #GaugeLiquiditys (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetGaugeLiquiditysHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, user = var_userdata_file = "result.txt")
#' result <- api_instance$CurveGetGaugeLiquiditysHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, user = var_user)
#' dput(result)
#'
#'
#' ####################  CurveGetGaugeTotalWeightsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#'
#' #GaugeTotalWeights (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetGaugeTotalWeightsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$CurveGetGaugeTotalWeightsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  CurveGetGaugeTypeWeightsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#'
#' #GaugeTypeWeights (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetGaugeTypeWeightsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$CurveGetGaugeTypeWeightsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  CurveGetGaugeTypesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_name <- "name_example" # character |  (Optional)
#'
#' #GaugeTypes (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetGaugeTypesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, name = var_namedata_file = "result.txt")
#' result <- api_instance$CurveGetGaugeTypesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, name = var_name)
#' dput(result)
#'
#'
#' ####################  CurveGetGaugeWeightVotesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_user <- "user_example" # character |  (Optional)
#'
#' #GaugeWeightVotes (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetGaugeWeightVotesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, user = var_userdata_file = "result.txt")
#' result <- api_instance$CurveGetGaugeWeightVotesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, user = var_user)
#' dput(result)
#'
#'
#' ####################  CurveGetGaugeWeightsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#'
#' #GaugeWeights (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetGaugeWeightsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$CurveGetGaugeWeightsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  CurveGetGaugeWithdrawsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#'
#' #GaugeWithdraws (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetGaugeWithdrawsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$CurveGetGaugeWithdrawsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  CurveGetGaugesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_address <- "address_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #Gauges (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetGaugesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetGaugesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetHourlyVolumesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #HourlyVolumes (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetHourlyVolumesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetHourlyVolumesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetLpTokensHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_address <- "address_example" # character |  (Optional)
#' var_name <- "name_example" # character |  (Optional)
#' var_symbol <- "symbol_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #LpTokens (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetLpTokensHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address, name = var_name, symbol = var_symbol, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetLpTokensHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address, name = var_name, symbol = var_symbol, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetPoolsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Pool address. (Optional)
#' var_name <- "name_example" # character | Pool's human-readable name. (Optional)
#'
#' #Pools (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetPoolsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, name = var_namedata_file = "result.txt")
#' result <- api_instance$CurveGetPoolsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, name = var_name)
#' dput(result)
#'
#'
#' ####################  CurveGetProposalVotesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#'
#' #ProposalVotes (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetProposalVotesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$CurveGetProposalVotesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  CurveGetProposalsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#'
#' #Proposals (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetProposalsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$CurveGetProposalsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  CurveGetRemoveLiquidityEventsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #RemoveLiquidityEvents (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetRemoveLiquidityEventsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetRemoveLiquidityEventsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetRemoveLiquidityOneEventsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #RemoveLiquidityOneEvents (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetRemoveLiquidityOneEventsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetRemoveLiquidityOneEventsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetSystemStatesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Singleton ID, equals to 'current'. (Optional)
#'
#' #SystemStates (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetSystemStatesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$CurveGetSystemStatesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  CurveGetTokensHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_address <- "address_example" # character |  (Optional)
#' var_name <- "name_example" # character |  (Optional)
#' var_symbol <- "symbol_example" # character |  (Optional)
#'
#' #Tokens (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetTokensHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address, name = var_name, symbol = var_symboldata_file = "result.txt")
#' result <- api_instance$CurveGetTokensHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address, name = var_name, symbol = var_symbol)
#' dput(result)
#'
#'
#' ####################  CurveGetTransferOwnershipEventsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #TransferOwnershipEvents (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetTransferOwnershipEventsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetTransferOwnershipEventsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetUnderlyingCoinsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer |  (Optional)
#' var_end_block <- 56 # integer |  (Optional)
#' var_start_date <- "start_date_example" # character |  (Optional)
#' var_end_date <- "end_date_example" # character |  (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #UnderlyingCoins (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetUnderlyingCoinsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetUnderlyingCoinsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' ####################  CurveGetVotingAppsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | App address. (Optional)
#' var_address <- "address_example" # character |  (Optional)
#'
#' #VotingApps (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetVotingAppsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_addressdata_file = "result.txt")
#' result <- api_instance$CurveGetVotingAppsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address)
#' dput(result)
#'
#'
#' ####################  CurveGetWeeklyVolumesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_pool <- "pool_example" # character |  (Optional)
#'
#' #WeeklyVolumes (historical) 🔥
#' api_instance <- CurveApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CurveGetWeeklyVolumesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pooldata_file = "result.txt")
#' result <- api_instance$CurveGetWeeklyVolumesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, pool = var_pool)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CurveApi <- R6::R6Class(
  "CurveApi",
  public = list(
    api_client = NULL,
    #' Initialize a new CurveApi.
    #'
    #' @description
    #' Initialize a new CurveApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Accounts (historical) 🔥
    #'
    #' @description
    #' Accounts (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param address (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveAccountDTO]
    #' @export
    CurveGetAccountsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetAccountsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, address, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Accounts (historical) 🔥
    #'
    #' @description
    #' Accounts (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param address (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveAccountDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetAccountsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["address"]] <- `address`

      local_var_url_path <- "/dapps/curve/accounts/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveAccountDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' AddLiquidityEvents (historical) 🔥
    #'
    #' @description
    #' AddLiquidityEvents (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveAddLiquidityEventDTO]
    #' @export
    CurveGetAddLiquidityEventsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetAddLiquidityEventsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' AddLiquidityEvents (historical) 🔥
    #'
    #' @description
    #' AddLiquidityEvents (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveAddLiquidityEventDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetAddLiquidityEventsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/addliquidityevents/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveAddLiquidityEventDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' AdminFeeChangeLogs (historical) 🔥
    #'
    #' @description
    #' AdminFeeChangeLogs (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveAdminFeeChangeLogDTO]
    #' @export
    CurveGetAdminFeeChangeLogsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetAdminFeeChangeLogsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' AdminFeeChangeLogs (historical) 🔥
    #'
    #' @description
    #' AdminFeeChangeLogs (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveAdminFeeChangeLogDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetAdminFeeChangeLogsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/adminfeechangelogs/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveAdminFeeChangeLogDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' AmplificationCoeffChangeLogs (historical) 🔥
    #'
    #' @description
    #' AmplificationCoeffChangeLogs (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveAmplificationCoeffChangeLogDTO]
    #' @export
    CurveGetAmplificationCoeffChangeLogsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetAmplificationCoeffChangeLogsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' AmplificationCoeffChangeLogs (historical) 🔥
    #'
    #' @description
    #' AmplificationCoeffChangeLogs (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveAmplificationCoeffChangeLogDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetAmplificationCoeffChangeLogsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/amplificationcoeffchangelogs/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveAmplificationCoeffChangeLogDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Coins (historical) 🔥
    #'
    #' @description
    #' Coins (historical) 🔥
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pool (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveCoinDTO]
    #' @export
    CurveGetCoinsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetCoinsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Coins (historical) 🔥
    #'
    #' @description
    #' Coins (historical) 🔥
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pool (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveCoinDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetCoinsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/coins/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveCoinDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' ContractVersions (historical) 🔥
    #'
    #' @description
    #' ContractVersions (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param address (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveContractVersionDTO]
    #' @export
    CurveGetContractVersionsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetContractVersionsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, address, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' ContractVersions (historical) 🔥
    #'
    #' @description
    #' ContractVersions (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param address (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveContractVersionDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetContractVersionsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["address"]] <- `address`

      local_var_url_path <- "/dapps/curve/contractversions/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveContractVersionDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Contracts (historical) 🔥
    #'
    #' @description
    #' Contracts (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveContractDTO]
    #' @export
    CurveGetContractsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetContractsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Contracts (historical) 🔥
    #'
    #' @description
    #' Contracts (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveContractDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetContractsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/curve/contracts/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveContractDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' DailyVolumes (historical) 🔥
    #'
    #' @description
    #' DailyVolumes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveDailyVolumeDTO]
    #' @export
    CurveGetDailyVolumesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetDailyVolumesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' DailyVolumes (historical) 🔥
    #'
    #' @description
    #' DailyVolumes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveDailyVolumeDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetDailyVolumesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/dailyvolumes/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveDailyVolumeDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Exchanges (historical) 🔥
    #'
    #' @description
    #' Exchanges (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveExchangeDTO]
    #' @export
    CurveGetExchangesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetExchangesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Exchanges (historical) 🔥
    #'
    #' @description
    #' Exchanges (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveExchangeDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetExchangesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/exchanges/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveExchangeDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' FeeChangeLogs (historical) 🔥
    #'
    #' @description
    #' FeeChangeLogs (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveFeeChangeLogDTO]
    #' @export
    CurveGetFeeChangeLogsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetFeeChangeLogsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' FeeChangeLogs (historical) 🔥
    #'
    #' @description
    #' FeeChangeLogs (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveFeeChangeLogDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetFeeChangeLogsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/feechangelogs/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveFeeChangeLogDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' GaugeDeposits (historical) 🔥
    #'
    #' @description
    #' GaugeDeposits (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveGaugeDepositDTO]
    #' @export
    CurveGetGaugeDepositsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetGaugeDepositsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' GaugeDeposits (historical) 🔥
    #'
    #' @description
    #' GaugeDeposits (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveGaugeDepositDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetGaugeDepositsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/curve/gaugedeposits/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveGaugeDepositDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' GaugeLiquiditys (historical) 🔥
    #'
    #' @description
    #' GaugeLiquiditys (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param user (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveGaugeLiquidityDTO]
    #' @export
    CurveGetGaugeLiquiditysHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, user = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetGaugeLiquiditysHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, user, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' GaugeLiquiditys (historical) 🔥
    #'
    #' @description
    #' GaugeLiquiditys (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param user (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveGaugeLiquidityDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetGaugeLiquiditysHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, user = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["user"]] <- `user`

      local_var_url_path <- "/dapps/curve/gaugeliquiditys/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveGaugeLiquidityDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' GaugeTotalWeights (historical) 🔥
    #'
    #' @description
    #' GaugeTotalWeights (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveGaugeTotalWeightDTO]
    #' @export
    CurveGetGaugeTotalWeightsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetGaugeTotalWeightsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' GaugeTotalWeights (historical) 🔥
    #'
    #' @description
    #' GaugeTotalWeights (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveGaugeTotalWeightDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetGaugeTotalWeightsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/curve/gaugetotalweights/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveGaugeTotalWeightDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' GaugeTypeWeights (historical) 🔥
    #'
    #' @description
    #' GaugeTypeWeights (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveGaugeTypeWeightDTO]
    #' @export
    CurveGetGaugeTypeWeightsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetGaugeTypeWeightsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' GaugeTypeWeights (historical) 🔥
    #'
    #' @description
    #' GaugeTypeWeights (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveGaugeTypeWeightDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetGaugeTypeWeightsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/curve/gaugetypeweights/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveGaugeTypeWeightDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' GaugeTypes (historical) 🔥
    #'
    #' @description
    #' GaugeTypes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param name (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveGaugeTypeDTO]
    #' @export
    CurveGetGaugeTypesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, name = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetGaugeTypesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, name, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' GaugeTypes (historical) 🔥
    #'
    #' @description
    #' GaugeTypes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param name (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveGaugeTypeDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetGaugeTypesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, name = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["name"]] <- `name`

      local_var_url_path <- "/dapps/curve/gaugetypes/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveGaugeTypeDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' GaugeWeightVotes (historical) 🔥
    #'
    #' @description
    #' GaugeWeightVotes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param user (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveGaugeWeightVoteDTO]
    #' @export
    CurveGetGaugeWeightVotesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, user = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetGaugeWeightVotesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, user, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' GaugeWeightVotes (historical) 🔥
    #'
    #' @description
    #' GaugeWeightVotes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param user (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveGaugeWeightVoteDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetGaugeWeightVotesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, user = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["user"]] <- `user`

      local_var_url_path <- "/dapps/curve/gaugeweightvotes/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveGaugeWeightVoteDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' GaugeWeights (historical) 🔥
    #'
    #' @description
    #' GaugeWeights (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveGaugeWeightDTO]
    #' @export
    CurveGetGaugeWeightsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetGaugeWeightsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' GaugeWeights (historical) 🔥
    #'
    #' @description
    #' GaugeWeights (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveGaugeWeightDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetGaugeWeightsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/curve/gaugeweights/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveGaugeWeightDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' GaugeWithdraws (historical) 🔥
    #'
    #' @description
    #' GaugeWithdraws (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveGaugeWithdrawDTO]
    #' @export
    CurveGetGaugeWithdrawsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetGaugeWithdrawsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' GaugeWithdraws (historical) 🔥
    #'
    #' @description
    #' GaugeWithdraws (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveGaugeWithdrawDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetGaugeWithdrawsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/curve/gaugewithdraws/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveGaugeWithdrawDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Gauges (historical) 🔥
    #'
    #' @description
    #' Gauges (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param address (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveGaugeDTO]
    #' @export
    CurveGetGaugesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetGaugesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, address, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Gauges (historical) 🔥
    #'
    #' @description
    #' Gauges (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param address (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveGaugeDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetGaugesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE








      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["address"]] <- `address`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/gauges/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveGaugeDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' HourlyVolumes (historical) 🔥
    #'
    #' @description
    #' HourlyVolumes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveHourlyVolumeDTO]
    #' @export
    CurveGetHourlyVolumesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetHourlyVolumesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' HourlyVolumes (historical) 🔥
    #'
    #' @description
    #' HourlyVolumes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveHourlyVolumeDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetHourlyVolumesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/hourlyvolumes/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveHourlyVolumeDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' LpTokens (historical) 🔥
    #'
    #' @description
    #' LpTokens (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param address (optional) 
    #' @param name (optional) 
    #' @param symbol (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveLpTokenDTO]
    #' @export
    CurveGetLpTokensHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, name = NULL, symbol = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetLpTokensHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, address, name, symbol, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' LpTokens (historical) 🔥
    #'
    #' @description
    #' LpTokens (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param address (optional) 
    #' @param name (optional) 
    #' @param symbol (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveLpTokenDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetLpTokensHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, name = NULL, symbol = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE










      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["address"]] <- `address`

      query_params[["name"]] <- `name`

      query_params[["symbol"]] <- `symbol`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/lptokens/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveLpTokenDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Pools (historical) 🔥
    #'
    #' @description
    #' Pools (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Pool address.
    #' @param name (optional) Pool's human-readable name.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurvePoolDTO]
    #' @export
    CurveGetPoolsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, name = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetPoolsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, name, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Pools (historical) 🔥
    #'
    #' @description
    #' Pools (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Pool address.
    #' @param name (optional) Pool's human-readable name.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurvePoolDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetPoolsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, name = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["name"]] <- `name`

      local_var_url_path <- "/dapps/curve/pools/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurvePoolDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' ProposalVotes (historical) 🔥
    #'
    #' @description
    #' ProposalVotes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveProposalVoteDTO]
    #' @export
    CurveGetProposalVotesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetProposalVotesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' ProposalVotes (historical) 🔥
    #'
    #' @description
    #' ProposalVotes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveProposalVoteDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetProposalVotesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/curve/proposalvotes/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveProposalVoteDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Proposals (historical) 🔥
    #'
    #' @description
    #' Proposals (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveProposalDTO]
    #' @export
    CurveGetProposalsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetProposalsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Proposals (historical) 🔥
    #'
    #' @description
    #' Proposals (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveProposalDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetProposalsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/curve/proposals/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveProposalDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' RemoveLiquidityEvents (historical) 🔥
    #'
    #' @description
    #' RemoveLiquidityEvents (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveRemoveLiquidityEventDTO]
    #' @export
    CurveGetRemoveLiquidityEventsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetRemoveLiquidityEventsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' RemoveLiquidityEvents (historical) 🔥
    #'
    #' @description
    #' RemoveLiquidityEvents (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveRemoveLiquidityEventDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetRemoveLiquidityEventsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/removeliquidityevents/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveRemoveLiquidityEventDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' RemoveLiquidityOneEvents (historical) 🔥
    #'
    #' @description
    #' RemoveLiquidityOneEvents (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveRemoveLiquidityOneEventDTO]
    #' @export
    CurveGetRemoveLiquidityOneEventsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetRemoveLiquidityOneEventsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' RemoveLiquidityOneEvents (historical) 🔥
    #'
    #' @description
    #' RemoveLiquidityOneEvents (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveRemoveLiquidityOneEventDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetRemoveLiquidityOneEventsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/removeliquidityoneevents/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveRemoveLiquidityOneEventDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' SystemStates (historical) 🔥
    #'
    #' @description
    #' SystemStates (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Singleton ID, equals to 'current'.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveSystemStateDTO]
    #' @export
    CurveGetSystemStatesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetSystemStatesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' SystemStates (historical) 🔥
    #'
    #' @description
    #' SystemStates (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Singleton ID, equals to 'current'.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveSystemStateDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetSystemStatesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/curve/systemstates/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveSystemStateDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Tokens (historical) 🔥
    #'
    #' @description
    #' Tokens (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param address (optional) 
    #' @param name (optional) 
    #' @param symbol (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveTokenDTO]
    #' @export
    CurveGetTokensHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, name = NULL, symbol = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetTokensHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, address, name, symbol, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Tokens (historical) 🔥
    #'
    #' @description
    #' Tokens (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param address (optional) 
    #' @param name (optional) 
    #' @param symbol (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveTokenDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetTokensHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, name = NULL, symbol = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE









      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["address"]] <- `address`

      query_params[["name"]] <- `name`

      query_params[["symbol"]] <- `symbol`

      local_var_url_path <- "/dapps/curve/tokens/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveTokenDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' TransferOwnershipEvents (historical) 🔥
    #'
    #' @description
    #' TransferOwnershipEvents (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveTransferOwnershipEventDTO]
    #' @export
    CurveGetTransferOwnershipEventsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetTransferOwnershipEventsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' TransferOwnershipEvents (historical) 🔥
    #'
    #' @description
    #' TransferOwnershipEvents (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveTransferOwnershipEventDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetTransferOwnershipEventsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/transferownershipevents/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveTransferOwnershipEventDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' UnderlyingCoins (historical) 🔥
    #'
    #' @description
    #' UnderlyingCoins (historical) 🔥
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pool (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveUnderlyingCoinDTO]
    #' @export
    CurveGetUnderlyingCoinsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetUnderlyingCoinsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' UnderlyingCoins (historical) 🔥
    #'
    #' @description
    #' UnderlyingCoins (historical) 🔥
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param pool (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveUnderlyingCoinDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetUnderlyingCoinsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/underlyingcoins/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveUnderlyingCoinDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' VotingApps (historical) 🔥
    #'
    #' @description
    #' VotingApps (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) App address.
    #' @param address (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveVotingAppDTO]
    #' @export
    CurveGetVotingAppsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetVotingAppsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, address, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' VotingApps (historical) 🔥
    #'
    #' @description
    #' VotingApps (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) App address.
    #' @param address (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveVotingAppDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetVotingAppsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["address"]] <- `address`

      local_var_url_path <- "/dapps/curve/votingapps/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveVotingAppDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' WeeklyVolumes (historical) 🔥
    #'
    #' @description
    #' WeeklyVolumes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CurveWeeklyVolumeDTO]
    #' @export
    CurveGetWeeklyVolumesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      local_var_response <- self$CurveGetWeeklyVolumesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, pool, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' WeeklyVolumes (historical) 🔥
    #'
    #' @description
    #' WeeklyVolumes (historical) 🔥
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) 
    #' @param pool (optional) 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CurveWeeklyVolumeDTO]) with additional information such as HTTP status code, headers
    #' @export
    CurveGetWeeklyVolumesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, pool = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["pool"]] <- `pool`

      local_var_url_path <- "/dapps/curve/weeklyvolumes/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CurveWeeklyVolumeDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
