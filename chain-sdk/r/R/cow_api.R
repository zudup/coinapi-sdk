#' On Chain - REST API
#'
#'  This section will provide necessary information about the `OnChain API` protocol.  <br/><br/> Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged. It was popularised in the 1960s with the release of Letraset sheets containing Lorem Ipsum passages, and more recently with desktop publishing software like Aldus PageMaker including versions of Lorem Ipsum.                             
#'
#' The version of the OpenAPI document: v1
#' Contact: support@coinapi.io
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Cow operations
#' @description CowApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ CowGetOrdersHistorical } \emph{ Orders (historical) }
#' Gets orders.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Cow.OrderDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CowOrderDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CowGetSettlementsHistorical } \emph{ Settlements (historical) }
#' Gets settlements.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } list( \link{Cow.SettlementDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CowSettlementDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CowGetTokensHistorical } \emph{ Tokens (historical) }
#' Gets tokens.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } address character
#' \item \emph{ @param } name character
#' \item \emph{ @param } symbol character
#' \item \emph{ @returnType } list( \link{Cow.TokenDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CowTokenDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CowGetTradesHistorical } \emph{ Trades (historical) }
#' Gets trades.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } sell_token character
#' \item \emph{ @param } buy_token character
#' \item \emph{ @returnType } list( \link{Cow.TradeDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CowTradeDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CowGetUsersHistorical } \emph{ Users (historical) }
#' Gets users.
#'
#' \itemize{
#' \item \emph{ @param } start_block integer
#' \item \emph{ @param } end_block integer
#' \item \emph{ @param } start_date character
#' \item \emph{ @param } end_date character
#' \item \emph{ @param } id character
#' \item \emph{ @param } address character
#' \item \emph{ @returnType } list( \link{Cow.UserDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CowUserDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CowOrdersCurrent } \emph{ Orders (current) }
#' Gets orders.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Cow.OrderDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CowOrderDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CowSettlementsCurrent } \emph{ Settlements (current) }
#' Gets settlements.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Cow.SettlementDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CowSettlementDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CowTokensCurrent } \emph{ Tokens (current) }
#' Gets tokens.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Cow.TokenDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CowTokenDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CowTradesCurrent } \emph{ Trades (current) }
#' Gets trades.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Cow.TradeDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CowTradeDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CowUsersCurrent } \emph{ Users (current) }
#' Gets users.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Cow.UserDTO} ) \cr
#'
#'
#' \item status code : 200 | successful operation
#'
#' \item return type : array[CowUserDTO]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  CowGetOrdersHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | User's address. (Optional)
#'
#' #Orders (historical)
#' api_instance <- CowApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CowGetOrdersHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$CowGetOrdersHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  CowGetSettlementsHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Transaction hash. (Optional)
#'
#' #Settlements (historical)
#' api_instance <- CowApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CowGetSettlementsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_iddata_file = "result.txt")
#' result <- api_instance$CowGetSettlementsHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id)
#' dput(result)
#'
#'
#' ####################  CowGetTokensHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | Token's address. (Optional)
#' var_address <- "address_example" # character | Token's address. (Optional)
#' var_name <- "name_example" # character | Token name fetched by ERC20 contract call. (Optional)
#' var_symbol <- "symbol_example" # character | Token symbol fetched by contract call. (Optional)
#'
#' #Tokens (historical)
#' api_instance <- CowApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CowGetTokensHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address, name = var_name, symbol = var_symboldata_file = "result.txt")
#' result <- api_instance$CowGetTokensHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address, name = var_name, symbol = var_symbol)
#' dput(result)
#'
#'
#' ####################  CowGetTradesHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer |  (Optional)
#' var_end_block <- 56 # integer |  (Optional)
#' var_start_date <- "start_date_example" # character |  (Optional)
#' var_end_date <- "end_date_example" # character |  (Optional)
#' var_id <- "id_example" # character |  (Optional)
#' var_sell_token <- "sell_token_example" # character |  (Optional)
#' var_buy_token <- "buy_token_example" # character |  (Optional)
#'
#' #Trades (historical)
#' api_instance <- CowApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CowGetTradesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, sell_token = var_sell_token, buy_token = var_buy_tokendata_file = "result.txt")
#' result <- api_instance$CowGetTradesHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, sell_token = var_sell_token, buy_token = var_buy_token)
#' dput(result)
#'
#'
#' ####################  CowGetUsersHistorical  ####################
#'
#' library(openapi)
#' var_start_block <- 56 # integer | The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock. (Optional)
#' var_end_block <- 56 # integer | The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock). (Optional)
#' var_start_date <- "start_date_example" # character | The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included. (Optional)
#' var_end_date <- "end_date_example" # character | The end date of timeframe. (Optional)
#' var_id <- "id_example" # character | User's address. (Optional)
#' var_address <- "address_example" # character | User's address. (Optional)
#'
#' #Users (historical)
#' api_instance <- CowApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CowGetUsersHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_addressdata_file = "result.txt")
#' result <- api_instance$CowGetUsersHistorical(start_block = var_start_block, end_block = var_end_block, start_date = var_start_date, end_date = var_end_date, id = var_id, address = var_address)
#' dput(result)
#'
#'
#' ####################  CowOrdersCurrent  ####################
#'
#' library(openapi)
#'
#' #Orders (current)
#' api_instance <- CowApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CowOrdersCurrent(data_file = "result.txt")
#' result <- api_instance$CowOrdersCurrent()
#' dput(result)
#'
#'
#' ####################  CowSettlementsCurrent  ####################
#'
#' library(openapi)
#'
#' #Settlements (current)
#' api_instance <- CowApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CowSettlementsCurrent(data_file = "result.txt")
#' result <- api_instance$CowSettlementsCurrent()
#' dput(result)
#'
#'
#' ####################  CowTokensCurrent  ####################
#'
#' library(openapi)
#'
#' #Tokens (current)
#' api_instance <- CowApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CowTokensCurrent(data_file = "result.txt")
#' result <- api_instance$CowTokensCurrent()
#' dput(result)
#'
#'
#' ####################  CowTradesCurrent  ####################
#'
#' library(openapi)
#'
#' #Trades (current)
#' api_instance <- CowApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CowTradesCurrent(data_file = "result.txt")
#' result <- api_instance$CowTradesCurrent()
#' dput(result)
#'
#'
#' ####################  CowUsersCurrent  ####################
#'
#' library(openapi)
#'
#' #Users (current)
#' api_instance <- CowApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CowUsersCurrent(data_file = "result.txt")
#' result <- api_instance$CowUsersCurrent()
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CowApi <- R6::R6Class(
  "CowApi",
  public = list(
    api_client = NULL,
    #' Initialize a new CowApi.
    #'
    #' @description
    #' Initialize a new CowApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Orders (historical)
    #'
    #' @description
    #' Orders (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) User's address.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CowOrderDTO]
    #' @export
    CowGetOrdersHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CowGetOrdersHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Orders (historical)
    #'
    #' @description
    #' Orders (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) User's address.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CowOrderDTO]) with additional information such as HTTP status code, headers
    #' @export
    CowGetOrdersHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/cow/orders/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CowOrderDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Settlements (historical)
    #'
    #' @description
    #' Settlements (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Transaction hash.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CowSettlementDTO]
    #' @export
    CowGetSettlementsHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      local_var_response <- self$CowGetSettlementsHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Settlements (historical)
    #'
    #' @description
    #' Settlements (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Transaction hash.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CowSettlementDTO]) with additional information such as HTTP status code, headers
    #' @export
    CowGetSettlementsHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      local_var_url_path <- "/dapps/cow/settlements/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CowSettlementDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Tokens (historical)
    #'
    #' @description
    #' Tokens (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Token's address.
    #' @param address (optional) Token's address.
    #' @param name (optional) Token name fetched by ERC20 contract call.
    #' @param symbol (optional) Token symbol fetched by contract call.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CowTokenDTO]
    #' @export
    CowGetTokensHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, name = NULL, symbol = NULL, data_file = NULL, ...) {
      local_var_response <- self$CowGetTokensHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, address, name, symbol, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Tokens (historical)
    #'
    #' @description
    #' Tokens (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) Token's address.
    #' @param address (optional) Token's address.
    #' @param name (optional) Token name fetched by ERC20 contract call.
    #' @param symbol (optional) Token symbol fetched by contract call.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CowTokenDTO]) with additional information such as HTTP status code, headers
    #' @export
    CowGetTokensHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, name = NULL, symbol = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE









      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["address"]] <- `address`

      query_params[["name"]] <- `name`

      query_params[["symbol"]] <- `symbol`

      local_var_url_path <- "/dapps/cow/tokens/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CowTokenDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Trades (historical)
    #'
    #' @description
    #' Trades (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param sell_token (optional) No description
    #' @param buy_token (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CowTradeDTO]
    #' @export
    CowGetTradesHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, sell_token = NULL, buy_token = NULL, data_file = NULL, ...) {
      local_var_response <- self$CowGetTradesHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, sell_token, buy_token, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Trades (historical)
    #'
    #' @description
    #' Trades (historical)
    #'
    #' @param start_block (optional) No description
    #' @param end_block (optional) No description
    #' @param start_date (optional) No description
    #' @param end_date (optional) No description
    #' @param id (optional) No description
    #' @param sell_token (optional) No description
    #' @param buy_token (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CowTradeDTO]) with additional information such as HTTP status code, headers
    #' @export
    CowGetTradesHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, sell_token = NULL, buy_token = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE








      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["sell_token"]] <- `sell_token`

      query_params[["buy_token"]] <- `buy_token`

      local_var_url_path <- "/dapps/cow/trades/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CowTradeDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Users (historical)
    #'
    #' @description
    #' Users (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) User's address.
    #' @param address (optional) User's address.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CowUserDTO]
    #' @export
    CowGetUsersHistorical = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, data_file = NULL, ...) {
      local_var_response <- self$CowGetUsersHistoricalWithHttpInfo(start_block, end_block, start_date, end_date, id, address, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Users (historical)
    #'
    #' @description
    #' Users (historical)
    #'
    #' @param start_block (optional) The start block. If endblock is not given, only those entities will be included that were exactly created in startBlock.
    #' @param end_block (optional) The end block. Useful to filter data in range of blocks (FROM startBlock TO endBlock).
    #' @param start_date (optional) The start date of timeframe. If endDate is not given, entities created FROM startDate TO startDate plus 24 hours will be included.
    #' @param end_date (optional) The end date of timeframe.
    #' @param id (optional) User's address.
    #' @param address (optional) User's address.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CowUserDTO]) with additional information such as HTTP status code, headers
    #' @export
    CowGetUsersHistoricalWithHttpInfo = function(start_block = NULL, end_block = NULL, start_date = NULL, end_date = NULL, id = NULL, address = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["startBlock"]] <- `start_block`

      query_params[["endBlock"]] <- `end_block`

      query_params[["startDate"]] <- `start_date`

      query_params[["endDate"]] <- `end_date`

      query_params[["id"]] <- `id`

      query_params[["address"]] <- `address`

      local_var_url_path <- "/dapps/cow/users/historical"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CowUserDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Orders (current)
    #'
    #' @description
    #' Orders (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CowOrderDTO]
    #' @export
    CowOrdersCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$CowOrdersCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Orders (current)
    #'
    #' @description
    #' Orders (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CowOrderDTO]) with additional information such as HTTP status code, headers
    #' @export
    CowOrdersCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/cow/orders/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CowOrderDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Settlements (current)
    #'
    #' @description
    #' Settlements (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CowSettlementDTO]
    #' @export
    CowSettlementsCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$CowSettlementsCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Settlements (current)
    #'
    #' @description
    #' Settlements (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CowSettlementDTO]) with additional information such as HTTP status code, headers
    #' @export
    CowSettlementsCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/cow/settlements/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CowSettlementDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Tokens (current)
    #'
    #' @description
    #' Tokens (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CowTokenDTO]
    #' @export
    CowTokensCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$CowTokensCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Tokens (current)
    #'
    #' @description
    #' Tokens (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CowTokenDTO]) with additional information such as HTTP status code, headers
    #' @export
    CowTokensCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/cow/tokens/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CowTokenDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Trades (current)
    #'
    #' @description
    #' Trades (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CowTradeDTO]
    #' @export
    CowTradesCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$CowTradesCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Trades (current)
    #'
    #' @description
    #' Trades (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CowTradeDTO]) with additional information such as HTTP status code, headers
    #' @export
    CowTradesCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/cow/trades/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CowTradeDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Users (current)
    #'
    #' @description
    #' Users (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CowUserDTO]
    #' @export
    CowUsersCurrent = function(data_file = NULL, ...) {
      local_var_response <- self$CowUsersCurrentWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Users (current)
    #'
    #' @description
    #' Users (current)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CowUserDTO]) with additional information such as HTTP status code, headers
    #' @export
    CowUsersCurrentWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/dapps/cow/users/current"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain", "application/json", "text/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CowUserDTO]", loadNamespace("openapi")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
