To run examples:

```bash
cd /path/to/workspace
git clone https://github.com/coinapi/coinapi-sdk
cd coinapi-sdk/cpp-rest
apt-get install libcpprest-dev
# update your API key inside coin_api_test.cpp
make
./coin_api_test
```
